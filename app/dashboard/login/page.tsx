'use client'; import { useState } from 'react'; import { MoonStar } from 'lucide-react'; import { useRouter } from 'next/navigation';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(r.ok)router.push('/dashboard');else setError('Email же сырсөз туура эмес.')}return <main className="grid min-h-screen place-items-center bg-navy p-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-luxury"><div className="grid h-12 w-12 place-items-center rounded-full bg-navy text-gold"><MoonStar/></div><h1 className="mt-6 text-2xl font-semibold text-navy">Admin Panel</h1><p className="mt-2 text-sm text-slate-500">Quran Academy башкаруу системасы</p>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<input name="email" type="email" required placeholder="Email" className="mt-6 w-full rounded-xl border p-3"/><input name="password" type="password" required placeholder="Сырсөз" className="mt-3 w-full rounded-xl border p-3"/><button className="mt-5 w-full rounded-xl bg-gold py-3 font-bold text-navy">Кирүү</button></form></main>}
