import { NextRequest,NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'; import { requireAdmin } from '@/lib/auth'; import { apiErrorResponse } from '@/lib/api-errors'; import { z } from 'zod'; const schema=z.object({title:z.string().min(2),description:z.string().min(2),icon:z.string(),topics:z.array(z.string()),order:z.number().int().optional()}); export async function GET(){return NextResponse.json(await prisma.program.findMany({where:{active:true},orderBy:{order:'asc'}}))} export async function POST(req:NextRequest){try{await requireAdmin(req);return NextResponse.json(await prisma.program.create({data:schema.parse(await req.json())}),{status:201})}catch(error){return apiErrorResponse(error,'Unable to create program')}}
